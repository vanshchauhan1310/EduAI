# ================================================================
# Admin Copilot — Requirements
# Python 3.13 + Windows x64 (Anaconda base — all pre-built wheels)
# Install: pip install -r requirements.txt --prefer-binary
# ================================================================

# ─── Web Framework ───────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ─── Database (MySQL local dev) ──────────────────────────────
sqlalchemy[asyncio]
aiomysql                 # async MySQL driver  (local dev)
pymysql                  # sync MySQL driver   (alembic migrations)
alembic

# NOTE — Production (Supabase): also install:
# asyncpg
# psycopg2-binary

# ─── Authentication & Security ───────────────────────────────
python-jose[cryptography]
passlib[bcrypt]
cryptography

# ─── Data Validation ─────────────────────────────────────────
pydantic
pydantic-settings
email-validator

# ─── AI Provider ──────────────────────────────────────────────
openai                        # OpenAI-compatible SDK for NVIDIA NIM
ollama                        # Legacy/local LLM option

# ─── Document Processing ─────────────────────────────────────
PyMuPDF                  # PDF text extraction
reportlab                # PDF generation
python-docx              # DOCX generation
Pillow                   # required by reportlab

# ─── Utilities ───────────────────────────────────────────────
python-dotenv
shortuuid
