# ================================================================
# Admin Copilot — Requirements
# Python 3.13 + Windows x64 (Anaconda base — all pre-built wheels)
# Install: pip install -r requirements.txt --prefer-binary
# ================================================================

# ─── Web Framework ───────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34
gunicorn>=23.0           # production ASGI server for Render
python-multipart

# ─── Database ────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0,<3.0
alembic>=1.14
asyncpg>=0.30            # async PostgreSQL driver (Supabase)
psycopg2-binary          # sync PostgreSQL driver  (alembic offline mode)

# ─── Authentication & Security ───────────────────────────────
python-jose[cryptography]
passlib[bcrypt]
cryptography

# ─── Data Validation ─────────────────────────────────────────
pydantic
pydantic-settings
email-validator

# ─── AI Provider ──────────────────────────────────────────────
openai                        # OpenAI-compatible SDK for NVIDIA NIM
ollama                        # Legacy/local LLM option
fastembed                     # Semantic embeddings via ONNX (replaces sentence-transformers + PyTorch — saves ~300 MB RAM)
huggingface_hub               # AI Tutor — HuggingFace Inference chat-completion client
requests                      # HTTP client used by AI and external service integrations
# ─── Document Processing ─────────────────────────────────────
PyMuPDF                  # PDF text extraction
reportlab                # PDF generation
python-docx              # DOCX generation
Pillow                   # required by reportlab
rapidocr-onnxruntime     # AI Tutor — optional OCR for scanned/symbol-font PDFs (lazily imported;
                         # missing only breaks OCR-flagged knowledge-base ingests, not normal ones)

# ─── Utilities ───────────────────────────────────────────────
python-dotenv
shortuuid


# ─── ML Requirement ───────────────────────────────────────────────
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
colorama==0.4.6
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.17
jiter==0.15.0
numpy==2.4.6
openai==2.38.0
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
python-dotenv==1.2.2
scipy==1.17.1
sniffio==1.3.1
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
xgboost==3.2.0
joblib
pandas
scikit-learn
openpyxl

